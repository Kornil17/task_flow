[project]
name = "task-flow"
version = "0.1.0"
description = "API для управления задачами"
readme = "README.md"
requires-python = "~=3.13.0"
dependencies = [
    "fastapi==0.136.3",
    "fastapi-code-generator==0.7.0",
    "pydantic==2.13.4",
]

[dependency-groups]
dev = []
